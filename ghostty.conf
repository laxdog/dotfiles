# This file is symlinked to $HOME/Library/Application\ Support/com.mitchellh.ghostty/config on MacOS
# Ghostty.app/Contents/Resources/ghostty/themes
theme=Dracula+
# Comments can be hard to see with Dracula
palette = 0=#71727c
# Alternative to above
# minimum-contrast = 1.4

# Fixes readline keys etc.
macos-option-as-alt = left
shell-integration-features = no-cursor
shell-integration = zsh
window-save-state = always

font-family = "Terminus (TTF)"
font-size = 13
font-thicken = false
window-inherit-font-size = false

cursor-style = block_hollow
cursor-style-blink = true

clipboard-trim-trailing-spaces = true
copy-on-select = clipboard

auto-update = check
auto-update-channel = tip

scrollback-limit = 134217728
term = xterm-256color

# Render behind the notch and don't use another desktop space
macos-non-native-fullscreen = true
